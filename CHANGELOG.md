# MD3Music 更新日志

各版本更新说明按 `## vX.Y.Z` 分组（与发布标签一致）。
发布时 CI 会自动取当前版本对应的节作为 GitHub Release 正文，并补上标题、构建时间、下载链接与页脚；
若某版本没有对应条目，则回退为自动生成的提交列表。

## v5.3.3

紧急修复部分 Android 14 机型（如 iQOO Neo9）启动即闪退的问题。

### 修复

- 修复 **Android 14 机型启动闪退**：高刷新率适配在设置「窗口触摸提频」时误用了 Android 15（API 35）才存在的方法 `setFrameRateBoostOnTouchEnabled`，而版本判断却只要求 Android 14 及以上，导致 Android 14 设备（如 iQOO Neo9）通过判断后调用不存在的方法而崩溃。已把门槛修正为 Android 15（API 35），并为高刷相关调用全部加上 `Throwable` 兜底——即便个别 ROM 缺方法也只降级为无触摸提频，绝不再影响 App 启动

## v5.3.2

本次更新恢复被覆盖的高音质修复，并优化添加到歌单的列表。

### 修复

- 恢复 **高音质（320k）无法播放** 的修复：v5.3.0 同步私有库功能时该修复被意外覆盖，高音质码回退为非法值 `hq`（酷狗合法码为 `320`）导致高音质取流失败。现已全链路恢复为 `320`：请求码、降级链、可用音质判定同步生效，老用户存储的遗留 `hq` 值自动迁移，设置页默认音质高亮恢复正常

### 优化

- 「添加到歌单」弹窗 **排除「我喜欢」歌单**：收藏该歌单走红心按钮一条路即可，弹窗不再出现重复入口（MD3/AM 两套播放器同时生效）

## v5.3.1

本次更新修复了字体设置被覆盖的问题，并优化了搜索页顶部样式。

### 修复

- 修复 **字体设置被覆盖**：「系统默认」字体模式下 CJK 文字沿回退链落到内置 SimHei，导致全局显示黑体、与「内置 SimHei」选项无差别。已将 SimHei 移出全局回退链，系统默认恢复设备系统字体，两个选项显示重新区分

### 优化

- 重构 **搜索页顶部样式**：搜索框条与结果分类栏改为固定纯色顶栏（不随滚动渐变、不叠模糊层），列表上滑被顶栏正常遮挡、文字清晰不发虚；实色底上的文字剥除全局光晕阴影
- 搜索框紧凑化：输入文字与提示语统一 13sp（此前输入文字默认 16sp 偏大），收紧清除按钮点击目标与图标内边距，框高 40→36
- 结果分类栏标签统一深色文字，选中/未选中由下划线指示器区分（此前未选中标签为浅灰、小字号下视觉发虚）
- 背景图解码共享：抽离 `backgroundImageProvider`，顶栏与页面主体命中同一 ImageCache 解码结果，避免同一张壁纸重复解码

## v5.3.0

本次更新带来了 **私人 FM 无限续播、音频共存、界面显示大小重做、Lyricon 词幕同步重构** 等多项新能力，同时整合了大量功能与细节优化，并修复了歌词匹配、账号切换、启动页闪屏等一系列问题。

### 功能

- 新增 **私人 FM**：发现页内嵌私人 FM 电台区块，支持 **无限续播**（播完自动补货续播）、卡片状态实时跟播当前曲目，顶栏改为问候胶囊
- 新增 **音频共存**：「忽略音频焦点」开关，开启后可与其它应用同时播放音频，微信语音、导航、通知等短促发声不再打断音乐
- 新增 **双击返回键回到桌面后台**：保留播放器与本地服务器运行，重新打开可瞬时恢复
- 重做 **界面显示大小**：改为无极缩放 + 10 秒自动复原保护，缩放同步作用于悬浮歌词
- 新增 **底部导航栏文字显示设置**：支持三档自由切换
- 新增 **内置默认壁纸**：未设置自定义背景图时自动兜底
- **Zen 模式** 改为长按专辑封面触发
- 评论区排序改为按热度
- LaunchPad 新增编辑模式与固定 / 未固定分区展示
- 新增 **歌词字重调节**
- 移植 LaunchPad / 编辑精选 / 听书 / 场景音乐 / 频道 五大功能
- 恢复 **悬浮窗识曲** 功能
- 同步一批功能：全局 Toast、全局背景图、刷刷、已购内容、听歌等级、多账号加密存储、MV 画中画、收藏按最近点击排序、相似歌单、桌面歌词等

### 歌词与词幕（Lyricon）

- 统一桌面歌词与播放器歌词的 **搜索词格式**（歌名+歌手），确保两处命中同一版本歌词，修复桌面歌词匹配与播放进度不同步
- 重构 Lyricon 播放状态同步：改用带时间戳的 **Auto PlaybackState 平滑走时**，消除 200ms 阶梯式卡顿
- 修复切歌后歌词不渲染、回退显示「作者-歌名」、以及 seek 后进度闪回的问题

### 修复

- 修复切换账号后仍显示旧账号的会员 / 等级 / 签到数据
- 修复 Android 启动页深色模式闪白屏（亮色纯白 / 暗色纯黑与 Flutter 首帧衔接）
- 修复媒体通知高频刷新导致的系统 UI 卡顿（更新频率降至 30 秒一次），并修正 seek 后通知进度
- 修复 Android 打包资源收缩导致快捷方式图标丢失
- 修复本地服务器启动失败处理与端口释放逻辑，避免请求长时间挂起
- 修复听书章节播放链接解析失败
- 移除「所有文件访问」「忽略电池优化」权限请求，仅保留必需权限

### 优化

- 优化全局背景模糊度与透明度默认值，完善退出动画与透明窗口背景
- 统一各页面搜索框样式与背景适配，优化边缘到边缘（edge-to-edge）布局
- 重构专辑卡片、播放器系统栏、收藏页与搜索结果页布局细节
- 优化本地 API 服务器性能与 Rust 构建参数（opt-level 3、保留导出符号）

## v5.2.1

本次更新是 5.x 系列的大版本，带来 **USB 独占音频输出、音乐频谱可视化、AI 歌曲推荐、MiniPlayer 上滑拖拽展开、封面动态取色与播放列表面板** 等多项重磅功能，并重构了 USB 音量管理、修复了线控唤醒与词幕连接等多项问题。同时补回了 v5.2.0 重构中丢失的「我的收藏」相关修复，确保收藏同步与批量删除功能完整可用。

### 功能

- 新增 **AI 歌曲推荐**：根据当前播放歌曲一键获取相似推荐，随时发现新歌
- 新增 **封面动态取色**：根据播放歌曲封面自动调整全局主题色，本地 / 网络封面统一取色口径
- 新增 **AM 风格播放列表面板**：支持拖拽重排与右滑删除，播放器内直接管理队列
- 新增 **USB 独占音频输出**：原生 USB 音频直写驱动（JNI + C++ usbdevfs），绕过系统混音，独占输出至 USB DAC
- 重构 **USB 音量管理**：普通 / 独占两种音量记忆自动切换，独占开启时自动切到 10% 初始音量防爆音，重启后保留配置
- 新增 **音乐频谱可视化**：环形柱状 / 曲线 / 背景条形三种频谱样式，适配 HyperOS 等 ROM，含权限适配与降级模拟模式
- 新增 **MiniPlayer 上滑拖拽展开**：拖拽跟手预览 + 判定规则，从迷你播放器直接过渡到全屏播放器
- **频谱功能增强**：新增透明度、动态取色与滑动切歌开关，优化绘制性能与暂停行为
- **本地音乐 Tab 默认开启**（其余可选 Tab 仍默认隐藏），全新安装即见本地歌曲

### 修复

- 修复 **线控唤醒需按两次才能播放**：headless 引擎改为同步消费媒体命令
- 修复 **词幕自动连接 / 断连重试**：headless 唤醒场景也能注册 MethodChannel，连接失败自动重试 3 次并提示
- 修复 **歌单随机播放顺序**：随机播放时加载歌单自动打乱，点击曲目保首曲、索引归零
- 修复 **MD3 长歌词显示不全**：行高按实际换行自适应，英文原词 + 翻译超长行完整显示
- 修复系统栏拖拽切换闪烁问题
- 修复 **收藏歌单 / 专辑 / 歌手不同步**：收藏页所有加载路径（进入页面 / 下拉刷新 / 收藏变更通知 / 离线重试）统一绕过本地代理缓存，收藏 / 取消收藏后进入或刷新页面即可看到最新数据
- 修复 **取消收藏歌手后仍停留在「收藏歌手」列表**：关注 / 取消关注歌手后立即同步刷新收藏歌手列表
- 修复 **收藏页 defunct setState 异常导致刷新失效**：补回 v5.2.0 重构中丢失的收藏同步修复后，收藏页刷新稳定可靠
- 恢复 **「我的收藏」专辑长按批量删除**：与歌单完全一致，可多选后批量取消收藏，并逐项统计成功 / 失败
- 完善 **删除歌单结果反馈**：批量删除逐项判定结果并统计，不再静默
- 更新登录错误解决方案提示

### 优化

- 优化播放控制按钮样式与频谱样式默认值（曲线模式）
- 调整主题默认种子色
- 重构歌曲页面状态管理，移除不必要的订阅
- 优化排行榜页面加载逻辑与 UI 样式
- 新增播放器拖拽展开单元测试
- 致谢 decent-player（USB 独占音频输出驱动来源）

## v5.1.2

本次更新修复了高音质（高品质 320k）无法播放，以及与其他软件同时发声时音乐被打断的问题。

### 修复

- 修复**高音质（高品质 320k）无法播放**：高音质请求码由错误的 `'hq'` 改为 KuGou API 正确的 `'320'`，高音质歌曲可正常播放；老用户已保存的高音质设置自动迁移
- 修复**与其他软件同时发声时音乐被打断**：通知、导航语音、微信语音等其他软件短促发声时，音乐保持播放不再暂停；仅电话来电、其他媒体应用抢占时才暂停（结束后自动续播）

## v5.1.1

本次更新修复「我的收藏」同步不及时与取消收藏歌手不消失的问题。

### 修复

- 修复收藏歌单 / 专辑 / 歌手后，收藏页需要手动下拉刷新甚至重启 App 才出现的问题：收藏页所有加载路径统一绕过本地代理缓存，收藏 / 取消收藏后进入或刷新页面即可看到最新数据
- 修复取消收藏歌手后歌手仍停留在「收藏歌手」列表的问题：关注 / 取消关注歌手后立即同步刷新收藏歌手列表

## v5.0.10

本次更新修复了两个显示问题。

### 修复

- 修复「系统默认」与「内置 SimHei」字体显示一致的问题：「系统默认」现在真正使用手机系统字体，与「内置 SimHei」的黑体渲染明显区分
- 移除「我的收藏」页歌单 / 专辑 / 歌手 Tab 下方的默认分隔细线，界面更干净

## v5.0.9

本次更新修复了歌单「定位正在播放歌曲」定位不准的问题。

### 修复

- 修复歌单内定位正在播放的歌曲经常不准（时远时近、有时滚到屏幕外）：改用 `GlobalKey` + 精确滚动替代固定行高估算，定位到目标歌曲后自动居中，不再受歌单头部区域与歌曲行高差异影响
- 随机播放 / 跨歌单场景下点击定位时，提示「当前播放歌曲不在本歌单中」，不再静默无响应

## v5.0.8

本次更新聚焦播放页歌词体验：新增「歌词动态颜色」与「歌词省电模式」两个开关，并全面优化歌词同步、渲染性能与功耗。

### 功能

- 新增**歌词动态颜色**：开启后当前行歌词根据专辑封面提取主色调混色（85% 白 + 15% 提取色），并兜底提升明度保证深色背景可读性（仅 AM 播放器）
- 新增**歌词省电模式**：歌词界面锁定 60fps 更省电，上下滑动歌词时自动解除限帧，保证滚动顺滑

### 性能与功耗

- 优化歌词渲染帧率：稳态演唱时不再每帧重建高斯模糊层（脏标记缓存）；非逐字歌词（LRC 逐行 / 纯文本）播放中画面静止时自动停止 Ticker 省电
- 优化辉光渲染：预渲染辉光精灵贴图替代每帧 `saveLayer + blur`，降低 GPU 开销
- 优化歌词滚动弹簧参数计算：输入缓存减少 `math.pow` 调用
- 优化流光背景：取色结果跨播放器往返缓存复用、进入播放器时延迟启动，降低进入瞬间的 GPU 峰值
- 降低播放器背景模糊计算量：全屏背景模糊 sigma 50 → 30，视觉不变但显著降低 GPU 峰值

### 体验优化

- 修正间奏点识别：KRC 行 duration 覆盖尾音 / 空白时，改用「人声实际结束时间」判定真实空档，间奏点不再漏识别
- 修正间奏点同步：seek / 跳转回跳时重置动画时钟；Ticker 切走 Tab / 退后台恢复后对齐真实播放进度，不再「跟不上」

## v5.0.7

本次更新为「我的收藏」的专辑页补上了与歌单一致的批量删除能力。

### 功能

- 「我的收藏」专辑支持**长按批量删除**：长按专辑进入批量管理模式（与歌单完全一致），可多选后批量取消收藏，逐项统计成功/失败
- 批量管理模式下切换 tab 自动退出选择状态，避免歌单/专辑选中错位

## v5.0.6

本次更新修复了「我的收藏」删除歌单失败的问题，并完善了批量删除的结果反馈。

### 修复

- 修复「我的收藏」删除歌单一直失败（提示「删除失败，请稍后重试」）：本地服务器转发删除接口时补齐了酷狗网关必需的 `x-router` 路由头，删除请求不再被网关 502 拒绝
- 修正批量删除歌单的 `type` 参数：删除自己创建的歌单用 `type=1`，取消收藏别人的歌单用 `type=0`，避免删错对象

### 体验优化

- 「我的收藏」批量删除改为逐项判定结果并统计：部分成功时提示「成功删除 N 个，M 个失败」，不再静默

## v5.0.5

本次更新修复了歌词动画的功耗问题，并优化了签到流程。

### 性能与功耗

- 修复播放歌词时 CPU 占用高、耗电的问题：歌词收敛检测只检查视口附近行，动画结束后 Ticker 能正常停止，不再持续高负载

### 签到优化

- 支持酷狗 20028 二次安全验证码流程：签到/领取遇到安全验证时自动弹出验证码，验证通过后自动重试，补齐畅听 VIP + 概念版双签到

## v5.0.4

本次更新重点优化了性能与功耗：降低播放时的 CPU / 温度 / 耗电，并大幅缩短 App 冷启动等待时间。

### 性能与功耗优化

- 修复播放时手机 CPU 占用高、发热、耗电的问题：播放进度更新不再导致播放页面整体高频重建，歌词动画空闲时自动进入省电态
- 播放进度条仍平滑更新，切歌、歌词、投屏、通知等功能不受影响

### 启动速度优化

- App 冷启动转圈时间从 4 秒+ 缩短到约 1.5 秒：本地服务检测更快，发现页改为渐进加载（先显示已获取的分区，其余后台补充）

## v5.0.3

本次更新主要优化了歌单的加载体验。

### 歌单体验

- 歌单详情（热门歌单 / 我收藏 / 发现等入口）改为**点进即加载全部歌曲**，无需分页滚动加载，进入即可浏览和搜索完整歌单

## v5.0.2

本次更新重点统一了界面风格并大幅优化了滚动性能：所有一级页面改为统一的 iOS 分组卡片风格，修复了列表快速滑动时的空白/卡顿问题，主题色新增自定义取色。

### 一、界面风格统一

- 设置、我的、发现、本地音乐、排行榜、我收藏、私人FM、封面流、搜索、听歌识曲 全部统一为 iOS 风格：中性背景 + 圆角分组卡片 + 语义色，浅深色自动适配
- 一级页面顶部标题居中显示
- 主题色新增「自定义颜色」圆形调色盘，可自由选取任意颜色作为主题色
- 封面流去掉背景块，更清爽

### 二、性能优化

- 歌单详情改为分页加载：首屏一页、滚动到底部自动加载下一页，不再一次性拉取全部歌曲
- 修复列表快速滑动时内容空白、停下才显示的问题（根源：列表项随播放进度高频重建、以及逐项入场动画）
- 封面图改用缓存加载并限制解码尺寸，滚动更流畅

### 三、其它

- 本地音乐移入可选 Tab（可在设置页「主页管理」中开启）
- 云盘、搜索等页面细节优化

## v5.0.1

本次更新重点补全了云盘音乐能力，并在播放、投屏、歌词与评论区做了大量优化。底层架构也完成了一次重要升级：本地服务器改用 Rust 全新重写，App 更小、启动更快、登录更稳定。同时修复了 v5.0.0 中云盘上传失败的问题。

### 修复

- **修复云盘上传**：移除 v5.0.0 引入的上传判断误判逻辑，恢复所有歌曲上传到云盘，并保留上传失败时的友好提示。

### 一、架构升级（重要）

- **底层服务器全面重写**：本地 API 服务器改用 Rust 实现，覆盖 160+ 酷狗接口，行为与酷狗官方对齐。不再依赖旧的 Node.js 运行时，App 体积更小、启动更快。
- **登录更稳定**：登录、注册、验证码等环节改为本地直连酷狗，不再依赖第三方转发。修复了登录二维码扫码后无法通过校验（20010）的问题。
- **启动不再失败**：本地服务器改用随机端口，彻底解决端口被占用导致 App 启动失败的问题。

### 二、云盘音乐（重点补全）

- **上传本地歌曲**：云盘页新增上传入口，支持长按多选、批量上传，上传失败会明确提示原因。
- **云盘歌曲显示封面**：自动从音频文件中提取内嵌封面，云盘列表不再"无封面"。
- **云盘歌曲可投屏**：云盘歌曲也能投放到电视、音响等 DLNA 设备。
- **浏览与播放更流畅**：优化云盘列表加载与分页，修复云盘歌曲播放失败（405）与无法切换音质的问题。

### 三、播放与投屏

- **本地歌曲投屏**：支持将本地音乐投放到 DLNA 设备（电视、音响等），可拖动播放进度，封面与歌名正常同步。
- **睡眠定时关闭**：播放器新增"定时关闭"，支持固定档位与自定义时长，到点自动暂停；顶部栏可随时查看并修改剩余时间。
- **无版权歌曲也能播**：已收藏的无版权歌曲可通过兜底链路正常播放；播放列表遇到无法播放的歌曲会自动跳过，不再卡死。
- **播放体验优化**：音质显示更真实（统一为"hq"），默认音质调整为 128kbps；拖动进度条时自动暂停，避免误听；搜索结果去重，同名歌曲不再重复展示。

### 四、歌词与视觉

- **逐字歌词更流畅**：大幅优化 Apple Music 风格歌词的渲染性能，字内渐变、上浮动画不再卡顿，视觉更细腻。
- **背景流光更好看**：优化动态背景取色逻辑，冷暖配色更协调，避免画面整体偏蓝绿。

### 五、评论区

- 热门评论与歌手评论置顶展示，带徽章标识
- 支持楼中楼评论分页加载、展开与收起
- 超长评论支持展开/收起动画
- 歌曲菜单新增"看评论"入口，评论面板实现统一
- 新增评论显示设置，可调整评论字号

### 六、问题修复

- 修复竖屏退出沉浸模式后状态栏不恢复的问题
- 修复播放列表页收藏按钮显示错误
- 修正"边听边存"默认状态（现默认为关闭）
- 更新用户协议页面的免责声明与内容
- 修正播放历史页相关收藏状态显示

### 七、工程与文档

- 新增 Windows 一键构建脚本，自动检测 Rust 变更并交叉编译
- 更新 README 与相关文档，适配新的架构说明
